fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlmodel==0.0.19
pydantic==2.7.4
pydantic-settings==2.3.4
jinja2==3.1.4
python-multipart==0.0.9
httpx==0.27.0
# Postgres driver (used when TG_DATABASE_URL points at Postgres; SQLite needs nothing).
psycopg2-binary==2.9.9
# Optional: enables the AWS cloud-VM provisioner. Safe to omit for local use.
boto3==1.34.131
# Browser-driven web evidence capture (run: playwright install chromium)
playwright==1.44.0
